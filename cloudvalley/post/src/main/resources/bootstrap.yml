spring:
  application:
    name: post
  cloud:
    nacos:
      server-addr: localhost:8848
      config:
        file-extension: yml
        namespace: dev
        group: dev

      discovery:
        server-addr: localhost:8848
        namespace: dev
        group: dev
  main:
    allow-bean-definition-overriding: true