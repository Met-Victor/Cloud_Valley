<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.com.system.mapper.UserMapper">

    <select id="getUserInfoList" resultType="cn.com.core.post.vo.UserVO">
        SELECT id,name,nickname,avatar,description,email FROM user where id in
        <foreach collection="list" item="item" index="index"
                 open="(" separator="," close=")">
            #{item}
        </foreach>
    </select>
</mapper>
